# Database

Este directorio concentra referencias relacionadas con Azure SQL y seeds.

- `seed_plans.sql`: ejemplo de seed directo para la tabla `planes`.
- Las migraciones reales viven en `backend/alembic/versions/`.
